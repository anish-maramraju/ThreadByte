import { motion } from 'motion/react';
import { Check, X } from 'lucide-react';

export function ComparisonSection() {
  return (
    <section className="relative py-20" style={{ background: 'rgba(30, 41, 59, 0.3)' }}>
      {/* Section removed - content will be updated later */}
    </section>
  );
}